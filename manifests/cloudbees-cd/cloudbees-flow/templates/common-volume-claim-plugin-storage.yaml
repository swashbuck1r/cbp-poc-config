---
# Source: cloudbees-flow/templates/common-volume-claim-plugin-storage.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "flow-server-shared"
  namespace: cloudbees-cd
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "5Gi"
